[project]
name = "solarmeter"
version = "1.0.0"
description = "Solar Meter Monitor"
requires-python = ">=3.11"
dependencies = [
    "requests",
    "flet"
]

[tool.flet]
product = "Solar Meter"

[tool.flet.app]
path = "."
module = "main"
